component: !mux
    ftrace:
        comp: 'ftrace'
        kself_args: 'summary=1'

run_type: !mux
    distro:
        type: 'distro'
    upstream:
        type: 'upstream'
        location: 'https://github.com/torvalds/linux/archive/master.zip'
