CONFIG_RCU_TORTURE_TEST enables an intense torture test of the RCU 
infratructure. It creates an rcutorture kernel module that can be 
loaded to run a torture test. The test periodically outputs status 
messages via printk(), test is started when the module is loaded, 
and stops when the module is unloaded
