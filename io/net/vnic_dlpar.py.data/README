Install Paramiko module:
Ubuntu :
-> apt-get install python-paramiko
-SLES/RHEL :
SLES :

-> SLES: Install python-devel,python-setuptools
--> RHEL: Install python-devel.ppc64le and python-setuptools.noarch
-> Download pycrypto-2.6.1.tar.gz & paramiko-1.16.0.tar.gz
-> For pycrypto : python setup.py build & python setup.py install
-> For parmaiko : easy_install ./
-> Then import paramiko.

RHEL :
-> yum install python-paramiko.noarch
Links :
-> https://pypi.python.org/packages/source/p/pycrypto/pycrypto-2.6.1.tar.gz#md5=55a61a054aa66812daf5161a0d5d7eda

input parameters:
hmc_ip: ltciofvtr-hmc.pok.stglabs.ibm.com
hmc_pwd: abc1234
hmc_username: hscroot
lpar: ltc-zz1b-lp11_vnic_automation
server: "ltc-zz1"
slot_num: 5
vios_name: ltc-zz1b-vios1
vios_id: 100
sriov_adapter_id: 1
sriov_port: 1
bandwidth: 4

input parameters:
lpar ---> lpar on which the vnic interface needs to be added/deleted
server ---> name of the server which hosts the lpar
slot_num ---> slot number on which the vnic interface will be added
vios_name ---> name of the vios used to add/remove vnic interface
vios_id ---> id of the vios
sriov_adapter_id ---> id of the the adapter that is assigned to the hypervisor
sriov_port ---> port using which the vnic interface will be added/removed
Ex: if the adapter has 2 ports and the vnic interface should be added using Port 1, mention value as 1 
else mention 2 if vnic interface should be added using Port 2
bandwidth ---> The percentage of the ports bandwidth this vNIC is entitled to
